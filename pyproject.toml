[project]
name = "mcp-weather-assistant"
version = "0.1.0"
description = "MCP weather assistant server backed by OpenWeather AI Assistant API"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.2.0",
    "requests>=2.31.0",
    "python-dotenv>=1.0.0",
    "openai>=1.40.0",
]

[tool.uv]
package = false
